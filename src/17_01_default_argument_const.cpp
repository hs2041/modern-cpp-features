#include <headers.h>

class abc{
    abc(int a){};
};
 
int main() 
{
    // The argument constructor prevents the compiler from creating a default argument less constructor
    // abc a;
    return 0;
}

